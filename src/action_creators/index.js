/*
* Action Creators
* are functions used to dispatch actions of the form { type, payload }
* `type` of the actions dispatched MUST have the name of the creator
* suffixed with action status if appropriate (e.g. PENDING, SUCCESS, FAILURE)
* all in FULL CAPS
* @example: function register dispatches REGISTER_PENDING, REGISTER_SUCCESS etc.
*/